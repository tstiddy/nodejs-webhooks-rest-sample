extends layout

block content
  h1 Listen to Webhook Events
  a(href='/disconnect') Disconnect from Office 365
  p.
    #{userId} waiting for notifications from subscription #{subscriptionId}...
  ul#events

  script(type='text/javascript' src='http://localhost:3001/socket.io/socket.io.js')
  script(type='text/javascript' src='/js/client.js')
  script(type="text/javascript").
    createSocketRoom('#{subscriptionId}');